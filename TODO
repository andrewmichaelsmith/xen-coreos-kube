Master should probably be MAC :00:00

git grep FIXME

gateway is the vm host, which is 122.1

node2/node3 need updating based on node1

#how to create kube-system namespace automatically?
curl -H "Content-Type: application/json" -XPOST -d'{"apiVersion":"v1","kind":"Namespace","metadata":{"name":"kube-system"}}' "http://127.0.0.1:8080/api/v1/namespaces"
